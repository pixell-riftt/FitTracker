@model IEnumerable<WorkoutIndexViewModel>

@{
    ViewData["Title"] = "All Workouts";
}

<div class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-list-ul"></i> All Workouts</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add Workout
        </a>
    </div>

    @if (Model.Any())
    {
        <div class="row">
            @foreach (var workout in Model)
            {
                <partial name="_WorkoutCardPartial" model="workout" />
            }
        </div>
    }
    else
    {
        <div class="alert alert-info text-center">
            <i class="bi bi-info-circle"></i> No workouts found. Be the first to add one!
        </div>
    }
</div>